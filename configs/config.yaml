server:
  port: 18080
  mode: debug

database:
  type: sqlite
  path: data.db

session:
  secret: change-this-secret-in-production
  max_age: 86400
  secure: false
  http_only: true
  same_site: lax

auth:
  default_admin:
    username: admin
    password: admin

log:
  level: info
  format: console
